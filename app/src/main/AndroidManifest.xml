<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions for xDrip HTTP fetching -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- Karoo data field permissions -->
    <uses-permission android:name="io.hammerhead.karoo.permission.DATA_FIELD" />
    <uses-permission android:name="io.hammerhead.karoo.permission.METRIC_ACCESS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KarooGlucometer"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.KarooGlucometer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Karoo Data Field Service -->
        <service
            android:name=".karoo.KarooDataFieldService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="io.hammerhead.karoo.DATA_FIELD_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="io.hammerhead.karoo.data_field_name"
                android:value="Glucose Monitor" />
            <meta-data
                android:name="io.hammerhead.karoo.data_field_description"
                android:value="Real-time blood glucose monitoring from xDrip+" />
            <meta-data
                android:name="io.hammerhead.karoo.data_field_version"
                android:value="1.0" />
        </service>
    </application>

</manifest>